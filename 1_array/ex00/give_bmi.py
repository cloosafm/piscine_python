import numpy as np
# Allowed functions : numpy or any lib of table manipulation


# BMI = weight (kg) / [height (m)]2
# to compute BMI in metric system : multiply height by itself and divide weight by the result
# for height = 180cm and weight = 78kg  => 78 / (1.8 * 1.8) = 24.074074074074073

def give_bmi(height: list[int | float], weight: list[int | float]) -> list[int | float]:
    """
    Calculate the BMI for each person in the list.
    """


def apply_limit(bmi: list[int | float], limit: int) -> list[bool]:
    """
    Check whether given BMI is above the limit ("True") or not ("False").
    """
    print("yo")


